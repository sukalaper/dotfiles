#!/usr/bin/env bash

# launch termite with other config
if [ "$1" == "-f" ]; then
	termite --class termite-float -c /home/anggiramadyansyah/.config/bspwm/termite/config&
else
	termite -c /home/anggiramadyansyah/.config/bspwm/termite/config&
fi
