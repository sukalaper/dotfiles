#!/bin/bash

style=$(cat $HOME/.config/dunst/sid)

if [[ "$style" == "Light" ]]; then
	brightnessctl s 5%- && notify-send "Brightness Dec : $(brightnessctl g)%"

else
	brightnessctl s 5%- && notify-send "Brightness Dec : $(brightnessctl g)%"
fi
